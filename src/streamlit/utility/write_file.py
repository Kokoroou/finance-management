from typing import Dict, Text, Any
import pandas as pd


DATA_PATH = "./../../../data/"


def write_file(data: Dict[Text, Any], file_name: Text) -> None:
    # TODO: Check if 'file_name' exists. If no, create a new file, else append to file

    # TODO: Check fields of 'data'. If a row exist, order to existed file's fields, else add a new column

    pass
